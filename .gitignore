# Python
venv/
__pycache__/
*.pyc

# 환경 설정 (토큰, 비밀번호 포함)
.env

# 사용자별 설정 (호칭, 워크스페이스 — 각 사용자마다 다름)
agent_config.json

# 런타임 데이터
mailbox.json
chat_history.json
*.pid
logs/
feedback/

# 버튼 이미지 (각 사용자가 직접 캡처해야 함)
images/*.png

# macOS
.DS_Store

# 카카오톡 토큰 (민감 정보)
kakao_tokens.json
